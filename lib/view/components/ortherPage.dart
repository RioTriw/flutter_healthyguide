import 'package:flutter/material.dart';

class OrtherPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Orther page"),
      ),
    );
  }
}
